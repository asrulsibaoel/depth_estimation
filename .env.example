# YOLO model path (relative to project base or absolute)
YOLO_MODEL_PATH=models/yolo11n.pt

# Depth model type: "midas" or "depth_pro"
DEPTH_MODEL_TYPE=midas

# Optional: Path to Depth Pro checkpoint if using depth_pro
DEPTH_PRO_CKPT=models/depth_pro.pt
